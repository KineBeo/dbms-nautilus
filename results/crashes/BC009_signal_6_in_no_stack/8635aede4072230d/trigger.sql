CREATE VIRTUAL TABLE IF NOT EXISTS fts_t1 USING fts3(c2);
EXPLAIN QUERY PLAN SELECT count(*) OVER () ORDER BY max(NOT FALSE < TRUE COLLATE BINARY) NOT IN (VALUES (CURRENT_DATE)) ASC NULLS LAST LIMIT FALSE;