CREATE TABLE IF NOT EXISTS p(a INTEGER PRIMARY KEY);
INSERT INTO p DEFAULT VALUES;
SELECT count(*) OVER (ORDER BY TRUE DESC NULLS LAST ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW EXCLUDE NO OTHERS) AS a;
CREATE VIRTUAL TABLE IF NOT EXISTS fts_t1 USING fts3(c2);
EXPLAIN QUERY PLAN SELECT count(*) OVER () ORDER BY max(CURRENT_TIMESTAMP COLLATE BINARY) NOT IN (VALUES (CURRENT_DATE)) ASC NULLS LAST LIMIT FALSE;