CREATE TABLE IF NOT EXISTS p(b TEXT NOT NULL);
CREATE INDEX IF NOT EXISTS idx1 ON p(b);
INSERT INTO p VALUES (NOT EXISTS (SELECT * FROM p NATURAL JOIN p GROUP BY FALSE)) ON CONFLICT DO NOTHING;
EXPLAIN QUERY PLAN SELECT DISTINCT count(DISTINCT CURRENT_TIME COLLATE RTRIM) LIKE CURRENT_DATE LIKE count(*) FILTER (WHERE CURRENT_DATE) OVER () ESCAPE TRUE ESCAPE FALSE FROM p;