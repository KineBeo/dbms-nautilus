# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/
.env
docs
CLAUDE.md

# Editor / OS
.DS_Store
*.swp

# Claude Code per-user state (NOT the skill, agent, or slash command — those are committed)
.claude/settings.local.json

# Superpowers brainstorm artifacts (mockup HTML, server state)
.superpowers/

# GitNexus index (regenerated locally by `npx gitnexus analyze`)
.gitnexus

# Generated grammar output — build artifact, not source
generated/
